# Daylight camera.
#
# Native resolution: a 25 x 12 m merchant at 7 NM is cd = sqrt(8.66 x 4.16) = 6.00 px,
# exactly the Johnson detection limit. Downsample in the pipeline, never render smaller.
kind = "eo"
hfov_deg = 49.0
width_px = 3840
height_px = 2160
