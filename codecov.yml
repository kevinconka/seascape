# Informational, never a gate: rendering needs a GPU that hosted runners do not
# have, so the measured number is structurally incomplete.
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

comment:
  require_changes: true
